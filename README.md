# Sentiment-Analysis-on-IMDB-Movie-Reviews
sentiment analysis project using the IMDB movie reviews dataset. Two different deep learning approaches are implemented and compared:  LSTM-based model (TensorFlow / Keras)  BERT-based transformer model (Hugging Face / PyTorch). The goal is to classify movie reviews as positive or negative.
